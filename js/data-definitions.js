/*
 * ==========
 * Data definitions:
 */
 
/* TaskState is one of:
 * - "TODO"
 * - "DONE"
 * - "DOING"
 *        
    function fnForTaskState(ts) {
        if (ts === "TODO") {
            //code  
        } else if (ts === "DONE") {
            //code  
        } else if (ts === "DOING") {
            //code
        }
    }
 */
 
/* ClassName is one of:
 * - "todo"
 * - "doing"
 * - "dono"
 * 
    function fnForClassName(cn) {
        if (cn === "todo") {
            //code  
        } else if (cn === "doing") {
            //code  
        } else if (ts === "done") {
            //code
        }
    }
 */

/* Node ...*/

/* ListOf(Nodes) /*